# Night log — 2026-08-16

What changed while you slept, what it measures, what I would do next. Five minutes.

## The short version

**All three example boards are committed fab-ready, and I verified it the hard way.**

| Board | Committed sidecar | Independent KiCad DRC, from-scratch on the packet in git |
|---|---|---|
| hydrate-coaster | `ready: true`, 0 blocking | **0 violations, 0 unconnected** |
| harness-puck | `ready: true`, 0 blocking | **0 violations, 0 unconnected** |
| terminal-keyboard | `ready: true`, 0 blocking | **0 violations, 0 unconnected** |

Not the sidecars' word for it — the gerber/KiCad packets pulled out of git and re-checked
by a tool that has never heard of our pipeline.

**Read the caveat before you act on it:** the *working tree* currently shows harness-puck at
136 blocking and terminal-keyboard at 12. Those are other agents' in-flight builds, not
regressions — `git show HEAD:` on both is clean. Do not judge the boards from the worktree
tonight; two of us were fooled by stale sidecars yesterday and I nearly moved a switch to fix
a defect that did not exist.

## The finding of the night: the via-in-pad defect was never real

I spent most of yesterday chasing a via sitting inside J1's USB-C VBUS pad. It was a phantom,
and the cause is worth knowing.

The pad polygon is written **closed** — the last vertex repeats the first. That is a
zero-length edge, and `@tscircuit/checks` decides "is this via inside this pad" with
`isPointOnSegment`, which on a zero-length segment returns true for *every point in the
plane* (`dot = 0`, `len² = 0`, so `0 <= 0 + 1e-9`). **The pad swallows the whole board** and
the spatial index hands it whichever via happens to share its grid cell.

Measured: 108 vias × 174 pads → **0 actual containments**. The named via clears the named pad
by 0.0008692 mm, on the outside. harness-puck and terminal-keyboard carry the same four
closed rings and passed only because none of their vias landed in the same bucket.

Fixed in two places: the rings opened in the golden block (geometrically a no-op — same
polygon, one fewer vertex), and a new **stage 0a** normaliser that opens any closed ring in
compiled circuit.json before anything reads it, then drops only the placement error its own
edit falsifies, judged on bbox containment so a genuine via-in-pad can never be dropped.
Imported EasyEDA/KiCad footprints close their rings routinely, so this protects every future
board.

**The lesson, recorded as ledger entry C:** a finding is a claim. The message contained both
coordinates. Measuring them against each other would have taken one minute on day one, and
would have saved most of a day of routing theories.

## Also landed

- **The EE's switch finding is fixed and, unusually, verified on the artifact** — commit
  `9869b37`: "Fix 1 is real on the artifact; three other lies on it were not." A stage 3b now
  holds the exported schematic to the design, so what your reviewer opens is what the board
  is. That was the loose end I was most worried about for today's meeting.
- **BOM/CPL now carry content, not just JLC's column order.** Comments come from the design
  (`1kΩ`, `100nF`, `12MHz`, `RP2040`); packages come from the catalog by LCSC number.
  Deliberately *not* inferred from geometry — the first attempt did that and called every 0402
  an 0603, because land patterns are bigger than the bodies they name.
- **The attempt logger is in before the algorithms it measures** — day 1 of the flywheel.
  Every routing attempt is one row: problem features, algorithm, seed, score, and the ruler
  hash. A tournament run that is not logged is a few thousand labelled examples thrown away.
- **`shift-left-check`: 53 fixes verified.** Up from 40 yesterday evening.

## Still running

- **Router tournament** — foundation + seven algorithm families + an independent judge + the
  portfolio selector. Resumed after the 02:40 limit reset; completed agents replay from cache.
- **EE-review workflow** — schematic verification, the 100×92 shrink, router seeding, diff-pair
  routing. Same resume.
- Hourly wakeup restarts whatever the limiter kills.

## What I would do next, in order

1. **Send the review packet.** All three boards are orderable and independently verified; the
   nine blocks are the thing that most needs a human eye and they have been ready for two days.
   `scripts/review-packet` regenerates the sheets.
2. **The honest first-build number.** `evals/agent/agent-eval-latest.json` is still the
   contaminated 14:12 file — `air-monitor` and `temp-logger` show `builds: 0`, which is the
   limiter killing them, not the platform failing. Until those re-run clean, we do not have a
   first-build fab-ready rate and should not quote one.
3. **Decide the keyboard's body.** The shrink is scoped precisely: the keys were never the
   problem (97.0mm of copper already fits inside 100), all 12mm is six screw bosses, and the
   fix is 100×92mm with the screws moved to top/bottom bands. $8.90 → $2.00 per five boards.
   It changes the printed body, which is why it is on your desk and not mine.
