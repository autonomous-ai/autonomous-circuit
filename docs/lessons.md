# The defect ledger

Every blocking defect we have hit on a real board, and **where the fix landed**.

The three example boards are the first three boards, not the deliverable. What
they are actually for is finding defect classes before a user does. So a defect
is not closed when the example stops failing — it is closed when a *future*
board cannot hit it.

Four places a fix can land, in descending order of value:

| Where | What it buys |
|---|---|
| **Planner** | the board can't be designed wrong in the first place |
| **Block / skeleton** | every board inheriting it is correct by construction |
| **`circuitlib`** | the number is right everywhere it is used, one owner |
| **Check only** | we find out afterwards, every time, forever |

**Check-only is not a fix.** It is a smoke alarm. A defect that is only detected
is still shipped by every user who ignores a warning, and still costs a build
cycle for every user who doesn't. It stays **open** in this ledger until it
moves up the table.

`scripts/shift-left-check` verifies that every fix claimed below actually exists
in the tree, and fails if one has been reverted or was never landed.

---

## Closed — a future board cannot hit these

| # | Defect | Found on | Fix landed in | Proof |
|---|---|---|---|---|
| 1 | Autorouter ran a GND track 0.115mm from the USB-C alignment drills; the fab needs 0.20mm. The router has no hole-clearance model at all. | all three | **Block** — a keepout sized to the receptacle's belly, inside the footprint so it travels with `pcbX`/`pcbY`/`pcbRotation` | `<keepout` in `usb-c-power.tsx` |
| 2 | A copper pour cuts a 32-sided polygon around every hole, so a nominal 0.2mm margin measures 0.1976mm at the chord midpoints — under the fab floor. Any board with any hole and a pour. | hydrate-coaster | **Glue block** — `POUR_CUTOUT_MARGIN_MM` derives the safe margin from the segment count, and the pour helper applies it | `POUR_CUTOUT_MARGIN_MM` in `blocks/glue.tsx` |
| 3 | `circuitlib.layout` stored each block's *size* and assumed its geometry was centred on the origin. It is not — `usb-c-data`'s copper sits 6.04mm above. Every board built on our own placement advice was wrong by millimetres before anyone wrote a line of TSX. | composition matrix (6/42 clean) | **`circuitlib`** — measured boxes relative to the origin, produced from real builds, plus `place_board()` which plans the whole board and returns its own warnings | `place_board` in `circuitlib/layout.py` |
| 4 | `autorouterEffortLevel` is a board prop with five settings and no CLI flag. We had never set it, so every board this project ever built routed at the default. At `"5x"`: terminal-keyboard 46 → 18 blocking, harness-puck 5 → 1, no design change. | all three | **Pipeline** — stage 0b retries at `5x` on a routing-class finding and keeps the retry only when it has strictly fewer blocking warnings | stage `0b` in `generation.py` |
| 5 | The planner could emit two USB-C entries on one board — `usb-c-data` is a superset of `usb-c-power` and reuses the same refdes block. | composition matrix | **Planner** — `board_plan` never picks both | the `usb-c-power` notes in `circuitlib/blocks.py` |
| 6 | `rp2040-core`'s crystal sat 11.78mm from XIN, past the 10mm limit, and the router silently skipped **the whole board**. | rp2040 boards | **Block** — placement corrected upstream | crystal placement in `rp2040-core.tsx` |
| 7 | A bare `<hole>` carries no keepout, so the router treats a mounting hole as free space. | all three | **Glue block + skeleton** — `MountingHole` ships the keepout, and a new project's first board already uses it | `MountingHole` in the project skeleton |
| 21 | The router's default effort produces boards that are not fab-ready, and **the escalation that exists to fix that cannot see the evidence**: stage 0b reads circuit.json, while the findings — `[clearance]`, `[shorting_items]`, `[hole_clearance]` — only exist after the KiCad cross-check three stages later. rp2040-core + `DebugPort`: `fab.ready` false with 5 blocking KiCad findings at default, true with 0 at `5x`, same design, same hour. | rp2040-core bench; all three boards | **Skeleton + skill + gauntlet** — every board declares `autorouterEffortLevel="5x"`, so the effort a board needs is a property of the board rather than a rescue the pipeline may not get to attempt. The escalation stays as the net for boards that do not. **`"10x"` was tried and dropped on measurement**: 46 minutes on harness-puck against 11 at `"5x"`, with the remaining findings unchanged in kind. A four-times serial cost for no measured gain is the wrong direction when the misses have a geometric fix (#28). | `autorouterEffortLevel` in the project skeleton |
| 22 | A via's *drill* was invisible to the hole-clearance gate — it only looked at component holes — so KiCad reported two violations at 0.132mm and 0.148mm on a board our own stage-4 gate called clean. Worse than a missed finding: a defect only KiCad can see arrives after the escalation gate, so the route is never retried. | rp2040-core bench | **`circuitlib`/profile + check** — `min_via_to_copper_mm = 0.20` (JLC "Via hole to Track"), and the gate now measures vias, SMD pads and tracks against every drill. Fires on the defect at stage 4a, in time to escalate; fires on none of the three boards. | `min_via_to_copper_mm` in `fab.py` |
| 23 | The hole-clearance gate modelled every drill as a circle of `hole_width/2`. The USB-C shell drills are 0.8 x 1.6mm **slots**, so copper off the end of one measured 0.4mm further away than it was. | reading the rule against the footprint | **Check** — drills, pads and slots are stadiums now, measured segment-to-segment | `_stadium` in `checks.py` |
| 24 | **The hole rule was applied to the hole's own pad**, so J1's shell-to-GND tie read as 0.006mm from J1's own GND drill and blocked two boards. That number is the annular ring. JLCPCB publishes "PTH annular ring >= 0.20mm" beside "PTH to Track 0.28mm"; both can be true only if the track figure measures copper arriving from outside. Measurement settles it: 28 segments across the three boards came within 0.28mm of a shell drill from outside its pad, and 25 of them measured 0.2000mm to four decimals — that is not geometry, it is where a connection crosses its own pad boundary, counted 25 times. A net-blind rule makes every plated hole unconnectable. KiCad's own hole-clearance DRC, on at 0.2mm and firing on NPTH holes on the same board, reported nothing at these pads. | harness-puck, hydrate-coaster | **Check** — same-net copper is exempt; with no net known either side, copper inside the pad's own outline is exempt; everything else still blocks. Both directions are pinned in the failure corpus. | `hole-rule-applied-to-the-hole-s-own-pad` in the failure corpus |
| 25 | The router halo (#21's sibling fix) was applied to **all four sides**, which put a USB-C receptacle's lowest copper 4.00mm inside the outline — a socket no cable reaches. The halo's evidence is about interior blocks; there is nothing to route in front of a connector. | `place_board` regression | **`circuitlib`** — the connector's face keeps `EDGE_MARGIN_MM`, the other three sides keep the halo | `face = EDGE_MARGIN_MM if edge` in `layout.py` |
| 9 | Silkscreen plotted at 0.033mm strokes; JLCPCB holds 0.15mm and thinner ink prints broken or not at all. **The real defect was worse than the symptom: all 54 silkscreen texts on the converted board are under JLCPCB's 1.0mm minimum height, 39 of them at 0.267mm, and none carries an explicit stroke — so KiCad derives one and plots it at a fifth of the floor. Every board this tool has ever made arrives with no readable reference designator.** | all three | **Pipeline (stage 3a)** — `kicad_normalize.normalize_for_fab` holds the converted board to the fab profile's silkscreen floors between the converter we do not own and the plotter. Idempotent, counted, reported. Measured after: 1864 of 1865 sub-floor strokes → 0 | `normalize_for_fab` in `circuitpy/kicad_normalize.py` |
| 8 | **The debug interface (SWCLK, SWD) reached no connector and no test point — the assembled board could not be halted, single-stepped or recovered from a bad image.** | all three | **Planner + glue block + skeleton.** The planner refuses the plan: `rp2040-core` declares `exposes=("SWCLK","SWD")`, `unexposed_nets()` reports what nothing brings out, `validate_board_law` makes it an **error**. `DebugPort` is what satisfies it — three 2.54mm pads, SWCLK/SWD/GND, beside `MountingHole` in the glue block. It is board furniture and **not** part of the MCU block, on measurement both ways: three pads inside `rp2040-core`'s own box send the debug pair through the crystal cluster and the router comes back with a via shorted into the QFN pad field; outboard, in open board space, the same design is `fab.ready` | `unexposed_nets` in `circuitlib/blocks.py`; `DebugPort` in `blocks/glue.tsx` |
| 13 | Copper sized for 604mA carrying 649mA, every segment one width. | harness-puck | **`circuitlib`** — `Block.peak_draw_ma` / `peak_per_unit_ma` give the planner a worst case (a WS2812 idles at 4mA and pulls 60mA), `board_plan` turns it into `power_trace_width_mm` by IPC-2221, and declaring less is an error | `power_trace_width` in `circuitlib/helpers.py` |
| 14 | An LDO reaching ~96 degC junction at the downstream chain's datasheet peak. | harness-puck | **Planner** — the same peak drives `regulator_thermal` at a 45 degC ambient, and a plan that would cook the part has `buildable = False`. Twenty WS2812s behind an AMS1117 is 1.3A and 183 degC: that board can no longer be planned | `overheats` in `circuitlib/helpers.py` |
| 15 | The pour-margin fix from #2 was structural but the number was still written down in three places: `hydrate-coaster` hardcoded `cutoutMargin="0.25mm"` on two pours, and two `BLOCK.md` files gave it as advice to remember. A number written down three times will disagree in two of them. | hydrate-coaster | **Delete the copies** — the pours pass `POUR_CUTOUT_MARGIN_MM`, and both BLOCK.md files now point at the constant instead of quoting it. `scripts/shift-left-check` fails if a literal comes back. | no `cutoutMargin="0.25mm"` literal outside the constant |
| 26 | **The supplier-footprint IoU band called eight identical capacitors a footprint mismatch because they were rotated.** Same part C1525, same `footprint="0402"`: thirteen at rotation 0 scored 0.7249, eight around an LED ring at multiples of 22.5 degrees scored 0.4739-0.4916, under the 0.5 error floor. The kind blocks, so **any board with an off-axis passive could never be fab-ready** — every circular layout, angled connector and diagonal-edge part, permanently un-orderable, and it looks like a real DFM problem every time. A bench of one part at six angles settles what the boards suggested: 0.7249 at 0, 90, 180 and 270; 0.4739 at 22.5; 0.4215 at 45. The metric is compared axis-by-axis, so it survives a quarter turn and collapses off-axis. | harness-puck | **Check** — orthogonal parts are graded in full; an off-axis part keeps its measurement at `info` with the angle and the bench numbers attached, reported rather than dropped. Lowering the floor past 0.47 was refused: it would keep the metric wrong and widen the hole for genuinely mismatched parts. Both directions are in the failure corpus, which now runs the IoU bander at all — it did not, which is how eight capacitors got there unopposed. | `footprint-iou-graded-through-a-rotation` in the failure corpus |
| 28 | **Two levers were pulled the wrong way for three hours because only the finding count was read, never the setting beside it.** (a) *Router effort is not monotonic.* harness-puck: 2 blocking at `10x`, 2 at `5x` — and 46 minutes against 11. hydrate-coaster: 2 at `10x`, **1** at `5x`. More search produced an equal or worse board at four times the wall clock. (b) *A smaller via pad made it much worse.* Reasoning from geometry, 0.50mm instead of 0.60mm buys 0.05mm of clearance per side and the remaining misses were all within 0.02mm of the floor; measured, harness-puck went **2 blocking to 17** — tracks crossing, five accidental contacts. The freedom was spent, not saved. | harness-puck, hydrate-coaster | **Both reverted, and the method changed with them.** Every effort or geometry knob is now quoted with its blocking count — `(effort, count)` — because a lever read one-sided is a lever nobody can tell is backwards. `5x` and a 0.60mm via pad are what the boards ship with, on measurement rather than on the argument that predicted otherwise. | `autorouterEffortLevel="5x"` in the project skeleton |
| 29 | **The schematic export drew every key as a short.** The 4-pad pushbutton renders on a 2-pin symbol, and `sw-tact`'s redundant same-net tie drew as a wire looping across it — the first human EE review read all 50 terminal-keyboard keys as dead. The copper was right; the artifact lied, and every representation (review SVG/PNG, KiCad export) inherits the same source, so no downstream check could see it. | terminal-keyboard, EE review 2026-08-15 | **Block + check** — the pushbutton declares `internallyConnectedPins` {1,2}/{3,4} (LCSC's own symbol for C318884 draws the bars; EasyEDA API, 2026-08-15), so every export folds same-terminal pins onto the symbol's two terminals and the loop is never drawn. Wiring went **diagonal** (signal → pin 1, pin 4 → to): pins 1/4 are opposite terminals under either side-pairing, so the "every board using it is scrap" pairing bet is gone from copper entirely — first-article continuity stays as the final check. `schematic_truth_warnings` (stage 1) flags a wire drawn between two pins of any switch symbol, on every board, forever. rp2040-core's BOOTSEL/RESET carried the same schematic defect and got the same `internallyConnectedPins` fix; its *copper* stays 4-tie **on measurement** (#28 method) — the diagonal rewire there landed a via 0.0787mm from the crystal trace, 5/5 runs, and same-group ties are safe under the confirmed pairing. | `internallyConnectedPins` in `sw-tact.tsx`; `schematic_truth_warnings` in `checks.py` |
| 30 | **The planner could not see fab price tiers.** 112×90mm quotes $8.90 for 5 sample boards; under 100×100 the same order is $2. Nothing at plan time knew the cliff existed, so nobody tried to fit it — 12mm cost 4.5×. | terminal-keyboard, EE review 2026-08-15 | **Planner + fab profile + check** — `JLC_SAMPLE_TIER_MM` in `circuitlib/tables.py` and `price_tier_warnings` inside `place_board`, so every plan is told the money before a board exists; `price_tiers` on the fab profile and `dfm_price_tier` at stage 4 name the overage and the measured quote on any board that misses every tier; ORDER.md's cost lines carry the cliff. **terminal-keyboard itself is now 100×90** (2026-08-16, Dee approved the body change): the "content spans 107.7mm" that read as a wall *was* the mounting holes — switch pads span 97.00mm and all copper 99.00mm, while the M2.5 drills span 107.70mm and their keepouts 108.20mm. Three moves, no pitch change: diodes tuck 2.0mm right (at −3.4 they hung 0.75mm off a 100mm board), the status LED shifts 2.2mm east (its courtyard was 0.320mm from the new edge), and all six screws leave the side rails for the top and bottom rails at x = −47/0/+47, y = ±42. No screw fits beside the key field at any pitch above 9.62mm, so the side bosses are gone from the printed body and the load moves to a rib on the bare underside — all 137 parts are top-side. Measured on the same pipeline, same command: **12 blocking → 1**, `dfm_price_tier` gone, 5 bare boards $8.90 → $2.00. | `price_tier_warnings` in `circuitlib/layout.py`; `price_tiers` in `fab.py` |
| 32 | **BOM/CPL ignored the consumer's spec.** JLC documents an exact sample shape — designators of one part grouped on one line, Rotation before Layer in the centroid — and we emitted what our tools produced naturally: one row per placement (50 identical switches = 50 lines) and the exporter's column order. | terminal-keyboard, EE review 2026-08-15 | **Exporters** — `group_bom_rows` gives every BOM one line per part with natural-sorted grouped designators (a part with no identity keeps its own line — two unknowns are not the same part); the CPL writer follows JLC's documented column order; `bom_summary` counts the grouped lines ORDER.md quotes while cost stays per placement. Golden-file tests pin the exact bytes. Applies to every board the app ever exports. | `group_bom_rows` in `fab.py`; `test_bom_golden_file_jlc_sample_shape` |
| 33 | **A polygon pad whose ring is closed reports every via on the board as inside it.** A polygon pad written the way every geometry format writes one — last vertex repeating the first — carries exactly one zero-length edge, and `@tscircuit/checks` resolves "is this via inside this pad" with a ray cast whose boundary shortcut `isPointOnSegment` answers **true for every point in the plane** against a zero-length segment: dot product 0, squared length 0, so `0 <= 0 + 1e-9` passes. The pad contains the board, and whichever via the spatial index reaches first is reported inside it. This is what blocked hydrate-coaster — `pcb_placement_error` on J1.B4A9 — and the geometry disproves the finding outright: the named via's center sits **0.3009mm above that pad's top edge**, with 0.0009mm of clear copper between its own annulus and the pad, and all 108 vias measured against all 174 pads gave **zero** containments. It is a landmine on every board with a USB-C port, not one board's bug: harness-puck and terminal-keyboard carry the same four closed rings and passed only because no via of theirs landed in the same spatial bucket as a connector pad. Two of three boards were fab-ready by luck. | hydrate-coaster; latent on all three | **Block + pipeline (stage 0a).** The golden block opens its four rings at source, so the library cannot ship one — the copper is identical, an open ring and a closed one describe the same polygon. And `normalize_circuit_json` opens any closed ring in the compiled circuit.json before anything reads it, because the shape arrives from outside too: a footprint imported from EasyEDA or KiCad closes its rings, and a user who pastes one in would hit the same false block with nothing to tell them why. The repair drops only the `pcb_placement_error` its own edit invalidates, judged on the most generous test available — the via center is not inside the pad's **bounding box** — so a genuine via-in-pad can never be dropped, and the corpus pins that direction too. A board with no closed ring is not rewritten at all, so the canonicaliser's byte conservatism survives wherever the defect is absent. Measured: `runAllChecks` on hydrate's circuit.json goes 2 findings to 1 with the rings opened, and the survivor is a trace length. The two frozen example snapshots (harness-puck, terminal-keyboard) still carry closed rings in their own `blocks/` copies; stage 0a covers them until they re-vendor. | `def normalize_circuit_json` in `circuitpy/circuit_normalize.py` |
| 34 | **Fix 1 reached the artifact; three other lies on the same artifact did not.** Verified on the 2026-08-15 export (build 22:07-22:26, later than every source file): SW10 renders as a two-pin switch, **pin 1 on K00, pin 2 on ROW0**, no wire across the contacts — all 50 keys, confirmed in KiCad's own netlist and by looking at the rendered symbol. The fold happens in tscircuit's schematic layout, not in our converter, which is why the converter has no reference to `internally_connected_source_port_ids` and the fix worked anyway. But the same file also carried: **`J1`, the USB-C receptacle, drawn as nothing at all** (`schematic_component.symbol_name` is null, the converter emits a `lib_id` it never defines, KiCad drops all 16 pins on the origin and reads VBUS/GND/D+/D− as **one net**); **`SW2` and `SW3` — BOOTSEL and RESET — with pin 1 dangling**, so the two buttons that kept 4-tie copper read as dead; and 13 more pins the design connects that KiCad reads as unconnected. An EE opening tomorrow's packet meets a shorted connector and two dead buttons. | terminal-keyboard, harness-puck, hydrate-coaster | **Pipeline (stage 3b)** — `kicad_schematic.normalize_schematic_truth` is `kicad_normalize`'s sibling for the schematic: it synthesises a body for any `lib_id` the converter references and never defines, pulls apart symbols stacked on one point, then compares KiCad's own netlist against `circuit.json` pin by pin and stamps the true net name where they disagree. Three sub-defects were found by measuring rather than reasoning. (a) *The drawn pin's identity is its **name**, not its number*: `Device:crystal_4pin_right` runs num 1..4 against name 4,2,1,3, so the number rule put XIN's label on a ground leg — the name resolves 239/239 pins on the three boards and disagrees with the number on exactly those 3. (b) *KiCad multiplies mm by 10000 to reach internal units and we divided by 0.0001*, which is a different computation in binary: 424.83124999999995mm is 4248312.5 IU multiplied (rounds up) and 4248312.499999999 divided (rounds down). One unit of 100nm was enough that **16 correct labels on harness-puck's J1 bonded to nothing** while the identical code repaired terminal-keyboard, whose coordinate happens to snap the other way. (c) *Three symbols on one point cannot be repaired with labels at all* — hydrate-coaster's TP1/TP2/TP3 all sat at (660.125, 496.5541), tying SWCLK, SWD and GND together, and a labelling pass loops forever on it; they get moved apart. Measured independently (KiCad's netlist against a union-find over circuit.json, not the pass's own report), before → after: nets covering >1 design net 0/0/1 → 0/0/0; design nets drawn as >1 2/4/2 → 0/0/0; pins the design connects that KiCad calls unconnected 14/15/15 → **0/0/0**. 6-12s a board, byte-identical on a second run. | `normalize_schematic_truth` in `circuitpy/kicad_schematic.py` |
| 35 | **No two builds of the same board ever produced the same file — and the route was never the reason.** "Fab-ready" was a property of a lucky run rather than of the design, which blocked caching, golden files and any regression test on a packet. The suspected cause was the router's wall clock: fewer solver steps fit into a slice on a busy machine, so the search lands somewhere else. Measurement says otherwise. hydrate-coaster at `"1x"`, six builds — three quiet at 95.9/95.5/97.6s and three with 16 busy processes alongside at 291.9/273.9/288.7s, a **2.9x wall-clock spread** — returned the **byte-identical route all six times** (`pcb_trace` + `pcb_via`, ids included), and **six different files**. Reading the code says the same thing: `tscircuit-cli` runs one 25.9MB bundle that requires nothing from node_modules, so the standalone `@tscircuit/capacity-autorouter` package where the clock calls were counted is never loaded; inside the bundle every solve loop is `for(;!this.solved&&!this.failed;)this.step()` against MAX_ITERATIONS, and the single real time budget in the whole bundle (PackSolver2's) reads `platform.pcbPackSolverTimeoutMs`, which nothing sets. The actual cause is the parts engine: 27 of hydrate-coaster's 1812 elements are `supplier_footprint_mismatch_warning`s, the only elements in the file whose id is not a counter, appended in **network-completion order** with a random 10-character id each. | all three | **Vendored toolchain patch + pipeline.** `toolchain/determinism/deterministic-run.mjs` replaces `Math.random` with a seeded generator, seeded from the board's own source fingerprint — a `NODE_OPTIONS --import` preload rather than a patched package, because the package is not what runs and forking a 25.9MB bundle would have to be redone on every version bump. Seeding fixes the id *stream*; it cannot fix *who gets which id*, so `_canonicalise_file` (stage 0, before anything reads the file) puts the racing elements in content order inside the slots they already occupy, and refuses any group whose ids are referenced, whose members are counter-numbered, or whose rewrite would not be a pure permutation. The preload **announces itself on stderr and the pipeline reports what it saw**, not what it asked for: `NODE_OPTIONS` is ignored the moment `tscircuit-cli` picks bun as its runner, and a flag that reports intent is how a fix goes quietly missing. Wall clock stays only as a safety valve — the 600s build budget and the 1500s escalation budget were both smaller than a loaded machine needs, so the machine, not the design, decided which route shipped; both are 5400s now. After: **one file hash across three quiet and three loaded builds**, and the route hash is unchanged from before the fix, so seeding moves no copper. **The boundary is the parts lock, and it is measured**: hydrate-coaster pins its 19 parts in `parts.json`, takes zero supplier round trips and is byte-identical; terminal-keyboard pins nothing, so every part goes to the catalogue and two builds came back with **47 and 17 `HTTP 429` rate-limit failures** — a different subset of parts resolved each time, 5459 elements against 5441. Its *route* is identical across all of it (`14d0f071`, five builds, 298–494s), but the two files are not builds of the same board and no seed can make them equal. Pinning terminal-keyboard's and harness-puck's parts is the rest of this fix, and it belongs to `parts-book`. | `_canonicalise_file` in `generation.py`; `toolchain/determinism/deterministic-run.mjs`; `toolchain/determinism/run_repeat.py` reproduces the six-build measurement and names the network when it is the cause |
| 38 | **The autorouter solves D+ and D− as two unrelated nets that happen to share a name.** EE review 2026-08-15, finding 3. Measured at 52d0803: hydrate-coaster 51.71/37.80mm, **7%** of the run coupled, widest gap 15.21mm; harness-puck 68.71/49.49mm, 1%, 16.98mm; terminal-keyboard 50.91/40.28mm, 25%, 7.76mm — against a straight-line distance of 23.05mm on hydrate. Two further blind spots came out with it: the coupling check matched its P/N token only at the *end* of a net name, so **`USB_DP_CONN`/`USB_DM_CONN` — the half of the pair closest to the cable — was never measured on any board**; and nothing measured the third thing the EE asked for, a return plane, which is **0% on all three boards**. | all three, EE review 2026-08-15 | **Pipeline (stage 0c) + check.** `circuitpy.diffpair.route_diff_pairs` runs after the autorouter and before anything reads the file: it deletes the pair's copper, finds one **centreline** with A* through a mask dilated by the pair's own half-span, and lays the two legs as that line offset both ways — parallel and constant-gap because they are the same polyline. Layer changes are allowed (a two-layer board with parts on top often has no top-side corridor at all: hydrate's is a 7.6mm² pocket) with one via per leg, and a **crossover** where the pair must swap sides — staggered vias, congruent diagonals, no skew added. It **refuses rather than regress**: every new segment is measured against every pad, drill, via, keepout, pour, other-net trace and the board edge at the fab profile's own floors, and the pair is only kept when coupling improves, skew does not, and clearance holds. Measured on hydrate-coaster: coupling **7% → 95%**, skew **13.91mm → 1.95mm** (inside the 3.8mm USB budget), lengths 51.7/37.8 → 37.0/39.0mm, worst clearance **0.115mm → 0.128mm**, 8 vias out and 8 in, 8s. harness-puck and terminal-keyboard refuse and say why. **Why it is not seeded best-of-N**: the capacity autorouter has no randomness — all 86 `Math.random` sites in the 25.9MB CLI bundle are id generation, React, three.js, undici and brace-expansion, none within 200kB of the autorouter, which agrees with #35's six-build measurement. **Why it is not pre-routing**: `pcbPath` does pin a trace's copper and take it out of the autorouter's list, but `getSimpleRouteJsonFromCircuitJson` builds obstacles from components, pads, plated holes, holes, vias and cutouts — **`pcb_trace` is not in that list**, so copper laid before the autorouter is copper it routes straight through. **And the pass is graded by the pipeline's gate, not by its own model** — which is what caught the first version. That one measured every clearance rule it had written down, passed its own tests, and produced a board with **eight `dfm_hole_clearance` errors and five KiCad hole-clearance violations**: at a 0.57mm pair pitch, one via's *pad* sits 0.196mm from the other via's *drill* against a 0.20mm rule. Pad-to-pad was legal and drill-to-drill was legal; the cross term was the one nobody had modelled. Two things landed from that: the pitch is now the max of all three rules (0.595mm, still inside the 4-width coupling budget), and `route_diff_pairs` takes a `grade` callback — the pipeline hands it `harvest_circuit_json` + `run_tscircuit_checks` + `dfm_warnings` and the rewrite is kept only when it adds no blocking finding the board did not already have. A gate that cannot run is a refusal, not a pass. **End to end on hydrate-coaster, same source, one build each**: `fab.ready` **true → true**, 0 errors → 0 errors, KiCad `drc_violation` 150 → 150, total warnings **166 → 165** — the pair's skew and coupling warnings both gone, and one new warn-band `dfm_annular_ring` in their place. That last one is a real tradeoff and the arithmetic is closed: the pair's own vias sit at the pair's pitch, so the pitch has to clear pad-to-pad, pad-to-drill and drill-to-drill *and* stay inside the coupling check's 4-width budget (0.60mm on 0.15mm copper). A via that trips none of the profile's warn bands needs a 0.50mm pad on a 0.30mm drill, which forces a 0.62mm pitch — outside the budget, so the pair could not change layers at all and hydrate-coaster would have no route. 0.45mm on 0.30mm keeps the preferred pad and the preferred drill and misses only the preferred annular ring, at 0.075mm — the fab's floor, not below it. | `route_diff_pairs` in `circuitpy/diffpair.py`; `netclass_pair_reference` in `verifylib/netclass.py` |
| 41 | **The hole-clearance gate did not know what shape a pad is, and the last blocking finding on the last board was a pad that is not there.** It modelled every SMD pad as the stadium inscribed in `width` x `height`, on the x-axis, and dropped the pad's own `ccw_rotation`. That does not blur a shape, it **moves** one. terminal-keyboard's U4 is a W25Q128 in SOIC-8: eight 2.25 x 0.63mm pads at `ccw_rotation: 90`, so the copper is 2.25mm *tall*. Swung back onto the x-axis it becomes 2.25mm *wide*, reaches 0.81mm sideways toward `pcb_via_200`, and reports **0.130mm against a 0.20mm floor**. The real gap is **0.506mm** — 3.9x the reported number and 2.5x the rule. Three independent witnesses: the 1.27mm pad pitch (eight pads 2.25mm wide at 1.27mm centres would overlap each other, so the unrotated reading cannot describe any real footprint); the KiCad packet, which writes every U4 pad with a trailing `90` in its `(at ...)` and `(size 2.25 0.63)`, so the plotted copper is the rotated one; and KiCad's own hole-clearance DRC, on at 0.2mm on the same packet, which reports **nothing** anywhere on the board. Reading the geometry took one minute and would have on day one — lesson C again, one shape further out. **Two more blind spots came out with it, both silent**: a `circle` pad carries `radius` and no `width`, and `width or radius` fed a *radius* into a *width*, so every round pad on every board was modelled at **half size** (the DebugPort test points are 1.0mm across and were measured as 0.5mm); and a `polygon` pad carries neither, failed the numeric guard, and **was never measured at all** — including the four shell pads on every USB-C receptacle we ship, which sit beside the exact drills this gate exists for. One invented finding, two hidden ones. | terminal-keyboard, 2026-08-16 | **Check** — `_pad_copper` reads the pad the fab plots: a rectangle keeps its corners, a pill is a stadium, a polygon is its own outline, a circle is its own radius, and every one of them is turned by its `ccw_rotation`. Drills get the same treatment. The exact model is *stricter* than the inscribed stadium it replaces (a 1.0mm square pad's corner protrudes 0.207mm past it), so it was measured before it was believed: **all three boards, 0 findings under the exact model, 0 under the old one plus the phantom** — the ruler got sharper in both directions and no board paid for it. Both directions are pinned in the failure corpus (`a-pad-measured-unrotated`: the same 0.130mm gap fires when the pad's long axis genuinely points at the drill, and terminal-keyboard's own geometry stays clean), and the three shape readings have unit tests. **`dfm_edge_clearance` had the identical blind spot and got the identical reading** — it too took `width`/`height` unrotated and skipped every polygon pad, which on these boards means the four USB-C shell pads, at the edge by design; measured the same way, the only change on the three boards is that those twelve pads are examined at all and all twelve pass. Free speedup as a side effect: a bounding-disc reject before the exact measurement takes the hole gate from **2.5s to 0.16s** on terminal-keyboard. | `_pad_copper` in `circuitpy/checks.py` |
| 10 | Mask openings separated by less than the 0.2mm sliver minimum; narrowest 0.114mm. | all three | **Not a defect — the check was wrong.** All ten sub-0.2mm webs sit inside *one part's own land pattern*: 0.114 and 0.157mm inside the USB-C receptacle's footprint, 0.1985mm inside each of eight 0402s, which is simply what a 0402 land pattern is. Those dams are specified by the package and JLCPCB builds them daily. The check is now scoped to webs between *different* parts — where nobody qualified the geometry — and that version fires on none of the three boards. Escalating the original would have made every board permanently un-orderable over a standard passive | `_mask_slivers` scoping in `verifylib/gerber_truth.py` |

---

## Open — detected, but a user can still hit them

These are the ones that matter now. Each is currently check-only.

| # | Defect | Found on | Where the fix belongs |
|---|---|---|---|
| 11 | A via whose center is *genuinely* inside an SMD pad — `C8.pin2`, 2026-08-11: via at (-26.55, -28.14) inside a 0402 pad centred (-26.51, -28.00). **This is not what blocked hydrate-coaster; that was #33's false positive arriving as the same kind, which is why the entry sat here for days without the real class moving.** Measured 2026-08-16 on the current board: 108 vias against 174 pads, zero containments — nothing is failing today, and the class is still only detected. | hydrate-coaster | **Pipeline or planner.** If the router does this unprompted it will do it on user boards. A real containment is a deterministic geometric repair like the via-bridge (#9's sibling in `kicad_normalize`), but it must validate the new position against every pad, track, via and pour on both layers first: an attempt that checked pads only produced six shorts, V3_3 to GND among them. |
| 27 | **The router-effort escalation cannot see the evidence it exists for.** Stage 0b decides from circuit.json, but the findings that say "route it differently" — `[clearance]`, `[shorting_items]`, `[hole_clearance]` — are KiCad's, three stages later. An rp2040-core board came back with five blocking findings, all five of them KiCad's, and the retry never fired. Every board now declares its own effort (#21), which is a floor a human chose rather than a rescue the pipeline performs. | rp2040-core bench; all three boards | **Pipeline.** Either run the KiCad cross-check before the escalation gate (it costs seconds, the retry costs minutes, and the fab costs two weeks) or move enough of its verdict into stage 4a that circuit.json alone can say "the route is wrong". #22 moved one — via drill clearance — and it now fires in time; `[clearance]` and `[shorting_items]` are still only KiCad's to see. |
| 12 | USB pair skew 18.88mm against a 3.8mm budget — and the EE confirmed the class (review 2026-08-15, finding 3): D+/D− must *travel as a pair*, which is geometry, not just length. **Now routed, not only reported, on the boards where a corridor exists** (#38). Still open because two of three boards have no corridor: the pair has to be routed *before* everything else for that, and the toolchain will not allow it. | terminal-keyboard, harness-puck | **v2 route stage** — a critical-nets pass that routes the pair *first*, so the corridor exists rather than has to be found afterwards; the planner-side length constraint remains `circuitlib`'s half. |
| 31 | **Power nets routed at signal width.** 5V/3V3 at 0.2mm — legal by current maths on this board, wrong as a class. Per-net width from current exists (ledger #13) but nothing distinguished a power net's *class* from a signal's. | terminal-keyboard, EE review 2026-08-15 | **Router / v2 route stage** — per-net-class widths the router applies. Until then it is detected on every board: `warn_power_trace_mm = 0.5` in the fab profile, `dfm_power_trace_width` at stage 4 (warn-band — the current-driven check still blocks a real overload; poured ground is exempt). An EE moves the line in the profile, one place. |
| 36 | **A board with no parts lock is not reproducible, and it ships anyway.** terminal-keyboard and harness-puck carry no `parts.json`, so every part is fetched from the supplier catalogue at build time. Two builds minutes apart took **47 and 17 `HTTP 429`** rate-limit refusals; each refusal is one part that silently loses its supplier footprint, and the build produces a packet regardless. The determinism symptom is the small half — the real one is that a fab-ready verdict can depend on how busy someone else's API was. | terminal-keyboard, harness-puck | **Parts lock (`parts-book`)** — pin every part to an exact LCSC number in `parts.json`, the way hydrate-coaster pins its 19 (zero round trips, byte-identical builds). Until then the harness reports it: `run_repeat.py` counts unresolved parts and 429s per run and names the network when the files differ for that reason. |
| 37 | **The drill-truth check does not know about our own via-bridge repair, and the repair does not honour the board's declared via rules.** `gerber_drill_extra` compares the drill file against `circuit.json`, but `kicad_normalize._fix_dead_end_vias` adds vias *after* circuit.json is written, so every board the bridge fires on reports drill hits "with no matching hole in the design — something was added between the design and the plot". Measured on terminal-keyboard at 100×90, twice: 228 drill hits against 225 designed features (213 vias + 8 holes + 4 plated), and the pipeline's own `kicad_normalized` line says "3 missing via(s) added under a B.Cu dead-end at a top-only pad" — the same three, at design (1.82, −28.77), (−19.87, −38.40) and (99.54, −136.65) in page space. The 112×90 board plotted 214 against 214 and bridged nothing, which is why nobody had met this. Two costs: a permanent false alarm on any board the bridge repairs, and no check at all that the extra hits *are* the bridge's. **And the vias it adds ignore the board's own declaration** — the source sets `minViaHoleDiameter="0.3mm"` and the packet carries a 0.15mm and a 0.20mm drill, because the bridge picks from its own size ladder. Both are legal at JLC (`min_via_drill_mm` 0.15) but neither is what the board asked for. | terminal-keyboard 100×90, 2026-08-16 | **Pipeline.** The repair already counts what it added (`Normalization.vias_bridged`); the drill-truth comparison has to be handed the post-repair hole set, or the repair has to write its vias back into the artifact of record. Its size ladder should start from the board's declared via geometry rather than a fixed list. |
| 39 | **A golden block moved its geometry and the board composing it was never rebuilt — so the packet in the repo says `fab.ready: true` for a board that today routes nothing at all.** The EE-review fix commits (4d9cec8, 3ce7e1c, 2026-08-15) re-vendored `rp2040-core` with the crystal cluster relocated from `pcbX=-8, pcbY=0` to `pcbX=0, pcbY=-10.5`. harness-puck composes that block at `pcbX=-2, pcbY=11`, which puts Y1's centre at (−2, +0.50) with a 3.6 × 2.9mm body against SW1 at (−2, −3.00) with a 7 × 4.4498mm body: the bodies overlap by 0.175mm and the courtyards by 0.900mm. tscircuit skips autorouting for the **whole board** on any placement error, so a rebuild from committed source produces **0 `pcb_trace` and 0 `pcb_via`** and 137 blocking findings (2 courtyard, 1 autorouting, 38 port-not-connected, 39 trace-missing, 1 dfa, 56 KiCad `unconnected_items`) — every one of them downstream of the single overlap. The committed packet was built 2026-08-13, before the move, and still claims 0 errors. Nothing gated it: the gauntlet builds each block's own testbench, the composition matrix builds block *pairs*, and neither builds the three examples. This is lesson D one level up — the ruler never looked at the thing that changed. | harness-puck, 2026-08-16 | **Gauntlet / CI** — a golden-block edit must rebuild every board that composes it before it can land, and a board whose packet is older than any of its inputs must not be publishable. The board-side lever is Dee's: SW1's position is product geometry (the key cap presses it), so the MCU cluster is what moves, not the button. |
| 40 | **`rp2040-core`'s own golden bench has been failing since 2026-08-11 and no run looked.** `test_bench_builds_clean[rp2040-core]` requires a golden bench to be warning-free; it reports `pcb_trace_too_long_warning — PCB trace is 12.46mm long, exceeding the 10mm maximum`. The geometry says which trace: the crystal cluster sits at `pcbY=-10.5` and the XOUT series resistor R11 at `pcbX=6`, so U3.XOUT → R11.pin1 spans √(6² + 10.5²) ≈ 12.1mm. It arrived with f5c6e54 (08-11 01:08, "Unblock ground planes and make RP2040 boards routable"), which is *after* the test landed (3e807e6, 08-10 22:50) — five days red. The same rule is violated on the boards, as a warning: terminal-keyboard **13.96mm**, hydrate-coaster **14.84mm**, both of which still route. That is worth stating precisely because ledger #6 says tscircuit "SKIPS AUTOROUTING FOR THE WHOLE BOARD" when the 10mm crystal maximum cannot be met — measured today it warns and routes, so #6's claim is either net-specific or overstated, and the block's own comment ("every endpoint within 9.1mm of XIN") is not true of XOUT. | rp2040-core bench; terminal-keyboard, hydrate-coaster | **Block** — put R11 inside the 10mm radius of XOUT rather than beside the crystal, and re-measure the route (#28 method: quote the lever with its blocking count). Until it moves, the suite that is supposed to make the library trustworthy is red, which is worse than the trace. |
| 42 | **A build does not use the checks in the tree, and nothing says so.** Two separate mechanisms, both of which hand back a packet with a fresh mtime and a verdict from a ruler that no longer exists. (a) *The cache does not know the ruler changed.* `_unchanged_prior_result` keys the idempotent short-circuit on the board's source fingerprint and the toolchain block — not on the check set — so fixing a check leaves every existing packet claiming what it earned under the old one, and a rebuild "correctly skips". The reverse is worse: a check that starts *catching* something re-judges nothing until a board's source happens to change. (b) *The pipeline that runs is a vendored copy.* `skills/circuitcode/scripts/packages/circuitpy/` is a generated copy of `packages/circuitpy/src/circuitpy/`, and it is what the CLI imports; `scripts/dev.sh` re-vendors on start but a direct build does not. Measured while closing #41: with the fix in the source and `CIRCUIT_FORCE_REGEN=1` set, a full 905-second rebuild returned **the identical blocking finding**, because the worker was running 2026-08-15's checks — the artifacts were fifteen minutes old and the verdict was a day old. Both are north-star's "every number carries the ruler it was measured with", from the side nobody watches. | terminal-keyboard, 2026-08-16 | **Pipeline** — put the check-set identity (the same hash the agent eval already prints) into the short-circuit key beside the source fingerprint and the toolchain block, and make the vendored runtime's identity part of it too, so a build either uses the tree's checks or refuses. Until then: `scripts/build/build-skill-runtimes.sh` then `CIRCUIT_FORCE_REGEN=1`, by hand, which is exactly the advice-a-user-has-to-remember this ledger exists to eliminate. |
| 16 | Raising silkscreen to the fab's 1.0mm floor (#9) made more of it land on pads: 70 strokes inside a mask opening became 99. Ink on a solderable surface stops the joint wetting, and JLCPCB clips it. | all three | **Block / layout.** The floor is not negotiable, so the text has to move: reference designators want a placement clear of the part's own pads, which is a footprint and `circuitlib.layout` question, not an exporter one. Created by a fix, and recorded rather than absorbed. |

---

## What these three boards taught about checks themselves

Not defects in a board — defects in the *way we look at boards*. Both were
caught before shipping, and both would have been invisible afterwards.

| # | Lesson | How it surfaced | What changed |
|---|---|---|---|
| A | **A check that cannot parse a shape silently passes it.** Our gerber reader could not evaluate parameterised aperture macros (`RotRect`, `RoundRect` — defined once, given their real numbers at each `%ADD%`), so it assigned them zero size and dropped them. **56 of 230 mask openings on harness-puck — a quarter of the layer, and every fine-pitch QFN pad — were never examined.** The sliver count went from "2" to the true 10 once they were resolved. | measuring a fix's before/after and finding the two numbers incomparable | macro parameters are substituted and rotation applied; nothing is dropped for being unparseable |
| B | **A fix can blind its own smoke alarm.** Setting `solder_mask_min_width` made KiCad plot the mask as 223 filled regions instead of 174 flashed apertures. The check only understood flashes, so it would have reported a clean mask on every board where the fix had been applied — quiet, not clean. | re-running the check on the fixed packet before believing the fix | the reader handles both representations, and the setting itself was **retracted**: measured against the same yardstick it removes no web at all, and costs an 8x slowdown. A change that alters the representation and not the board is a placebo |
| C | **A finding is a claim, and the first thing to do with it is measure it.** "Via inside SMD pad" was read as a router defect and answered with router work — effort levels, clearances, `allowViaInPad`, freerouting, a ground plane, and a nudge script that created six shorts. None of it could have worked: the via named by the finding was 0.3009mm outside the pad named by the finding, and no via on the board was inside any pad. Twenty lines of Python over the artifact of record said so in under a minute, and would have said so on day one. | measuring the two coordinates in the message against each other before touching the router | every via-in-pad claim is now re-measured against the geometry, and one whose own numbers disprove it is dropped with the reason recorded (#33) |
| E | **A repair pass graded by its own model is exactly as right as its model.** The differential-pair pass measured every clearance rule it knew — copper to copper, copper to drill, drill to drill, edge, keepout, pour — refused anything that broke one, passed its own unit tests, and turned a `fab.ready` board into a board with 14 blocking findings. The rule it did not know was the *cross* term: one via's pad against another via's drill, 0.196mm against a 0.20mm floor. Nobody would have written that rule down, because it only exists where two vias of one net sit at a pair's pitch. | running the whole pipeline on the pass's output instead of trusting the pass's own verdict | the pass takes a `grade` callback and the pipeline hands it the real gate; the rewrite is kept only when it adds no blocking finding the original did not have, and a grader that raises is a refusal. Every repair that writes copper should be held this way |
| D | **A determinism harness called an empty route reproducible.** The first thing `run_repeat.py` was pointed at was harness-puck, and it reported *route bytes IDENTICAL* over three runs. The board had routed nothing: a courtyard overlap (Y1 against SW1) makes tscircuit skip autorouting for the whole board, so circuit.json carried zero `pcb_trace` and zero `pcb_via`, and the sha256 of nothing is stable forever. It is lesson A one level up — the ruler could not see the shape it was measuring, so it passed it. | pointing the new ruler at a board before checking the board | the harness refuses to report on a run with no copper or a `pcb_autorouting_error`, prints the build's own messages, and exits non-zero; every report now carries the effort level, the copper count and the load average it was taken at |


## The rule

When a board fails and an agent fixes it, the question is never "is the board
fixed". It is **"why did the first composition get this wrong, and what stops
the next one"**. The fix belongs in the planner, the block, the skeleton or
`circuitlib` — never in advice a user has to remember, and never only in the
example that happened to catch it.

A block that needs correct handling to be safe is a block that will be handled
wrong.
