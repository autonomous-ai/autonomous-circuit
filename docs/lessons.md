# The defect ledger

Every blocking defect we have hit on a real board, and **where the fix landed**.

The three example boards are the first three boards, not the deliverable. What
they are actually for is finding defect classes before a user does. So a defect
is not closed when the example stops failing — it is closed when a *future*
board cannot hit it.

Four places a fix can land, in descending order of value:

| Where | What it buys |
|---|---|
| **Planner** | the board can't be designed wrong in the first place |
| **Block / skeleton** | every board inheriting it is correct by construction |
| **`circuitlib`** | the number is right everywhere it is used, one owner |
| **Check only** | we find out afterwards, every time, forever |

**Check-only is not a fix.** It is a smoke alarm. A defect that is only detected
is still shipped by every user who ignores a warning, and still costs a build
cycle for every user who doesn't. It stays **open** in this ledger until it
moves up the table.

CI runs `scripts/shift-left-check` to verify that every fix claimed below
actually exists in the tree, and fails if one has been reverted, re-copied, or
was never landed. The checker also requires exact set equality between numeric
rows in this Closed table and executable ratchet IDs, so adding prose without a
proof is itself a CI failure.

---

## Closed — a future board cannot hit these

| # | Defect | Found on | Fix landed in | Proof |
|---|---|---|---|---|
| 1 | Autorouter ran a GND track 0.115mm from the USB-C alignment drills; the fab needs 0.20mm. The router has no hole-clearance model at all. | all three | **Block** — each 0.6mm NPTH has an exact 1.02mm component-local guard, inside the footprint so drill and guard transform together while the legal central routing pocket stays open | the paired alignment-drill `<keepout>` elements in `usb-c-power.tsx` |
| 2 | A copper pour cuts a 32-sided polygon around every hole, so a nominal 0.2mm margin measures 0.1976mm at the chord midpoints — under the fab floor. Any board with any hole and a pour. | hydrate-coaster | **Glue block** — `POUR_CUTOUT_MARGIN_MM` derives the safe margin from the segment count, and the pour helper applies it | `POUR_CUTOUT_MARGIN_MM` in `blocks/glue.tsx` |
| 3 | `circuitlib.layout` stored each block's *size* and assumed its geometry was centred on the origin. It is not — `usb-c-data`'s copper sits 6.04mm above. Every board built on our own placement advice was wrong by millimetres before anyone wrote a line of TSX. Later, the planner selected `usb-power-entry` while the measured-box table omitted it, so a valid plan still crashed at placement. | composition matrix (6/42 clean) | **`circuitlib`** — measured boxes relative to the origin, produced from real builds, plus `place_board()` which plans the whole board and returns its own warnings. A registry-set regression now requires a measured box for every plannable block. | `test_measured_extents_are_plausible` in `test_circuitlib.py`; `evals/measure_block_boxes.py` |
| 5 | The planner could emit two USB-C entries on one board — `usb-c-data` is a superset of `usb-c-power` and reuses the same refdes block. | composition matrix | **Planner** — `board_plan` never picks both | the `usb-c-power` notes in `circuitlib/blocks.py` |
| 6 | `rp2040-core`'s crystal sat 11.78mm from XIN, past the 10mm limit, and the router silently skipped **the whole board**. | rp2040 boards | **Block** — placement corrected upstream | crystal placement in `rp2040-core.tsx` |
| 7 | A bare `<hole>` carries no keepout, so the router treats a mounting hole as free space. | all three | **Glue block + skeleton** — `MountingHole` ships the keepout, and a new project's first board already uses it | `MountingHole` in the project skeleton |
| 11 | A via placed inside an SMD pad (`C8.pin2`). The post-route checker caught it, but the router's exact evaluator assigned it zero cost and would regenerate it on future boards. | hydrate-coaster | **Pipeline** — exact via-to-SMD clearance steers movable vias outside the pad; an algorithm-independent core boundary rejects unresolved/cached/Pipeline9/fanout output before copper export. Explicit connected via-in-pad and legal PTH behavior remain opt-in/preserved. CI runs the complete serial `scripts/tests/*.test.mjs` toolchain suite, so this gate and its cache/fanout siblings cannot disappear behind a narrower smoke test. | `CAPACITY_VIA_IN_SMD_PAD_PREVENTION_PATCH`, `CORE_VIA_IN_SMD_PAD_OUTPUT_GATE_PATCH`, `via-in-smd-pad.test.mjs`, and the CI router-suite glob |
| 29 | A source trace requested 0.25mm copper, the phase SRJ retained 0.25mm, and Pipeline7 silently emitted 0.15mm because its width solver treated the per-connection value as a preference. Restoring 0.25mm exposed real clearance conflicts, so output could not simply be widened after routing. | golden USB composition | **Pipeline + block** — the router now treats an explicit source width as a hard minimum, removes the thinner fallback and terminal taper, and fails the phase when that width cannot fit. The reusable USB geometry was rebuilt until both faces routed at the declared widths: CC 0.25mm, differential pair 0.15mm, and power 0.2/0.8mm. | `CAPACITY_EXPLICIT_TRACE_WIDTH_PATCH`; `explicit-trace-width.test.mjs`; `test_usb_data_bottom_routed_contract_matches_top` |
| 22 | A via's *drill* was invisible to the hole-clearance gate — it only looked at component holes — so KiCad reported two violations at 0.132mm and 0.148mm on a board our own stage-4 gate called clean. Worse than a missed finding: a defect only KiCad can see arrives after the escalation gate, so the route is never retried. | rp2040-core bench | **Fab profile + independent check** — `min_via_to_copper_mm = 0.20` (JLC "Via hole to Track"), and stage 4 measures via drills against different-net trace, SMD-pad, and via copper. It blocks both observed defects early enough to escalate and remains clean on Harness and Hydrate. | `min_via_to_copper_mm` in `fab.py`; the 0.132mm/0.148mm failure-corpus cases |
| 23 | The hole-clearance gate modelled every drill as a circle of `hole_width/2`. The USB-C shell drills are 0.8 x 1.6mm **slots**, so copper off the end of one measured 0.4mm further away than it was. | reading the rule against the footprint | **Independent check** — round and slotted drills are rotation-aware swept stadiums, measured segment-to-segment, so a USB slot endpoint cannot disappear. | `_stadium` in `checks.py`; `usb-c-slot-endpoint-modeled-as-a-circle` in the failure corpus |
| 24 | **The hole rule was applied to the hole's own pad**, so J1's shell-to-GND tie read as 0.006mm from J1's own GND drill and blocked two boards. That number is the annular ring. JLCPCB publishes "PTH annular ring >= 0.20mm" beside "PTH to Track 0.28mm"; both can be true only if the track figure measures copper arriving from outside. Measurement settles it: 28 segments across the three boards came within 0.28mm of a shell drill from outside its pad, and 25 of them measured 0.2000mm to four decimals — that is not geometry, it is where a connection crosses its own pad boundary, counted 25 times. A net-blind rule makes every plated hole unconnectable. KiCad's own hole-clearance DRC, on at 0.2mm and firing on NPTH holes on the same board, reported nothing at these pads. | harness-puck, hydrate-coaster | **Independent check** — same-net, same-port, and own-feature copper is legal; when identity is absent, fallback exempts only copper wholly contained by that drill feature's own annular pad. Foreign copper still blocks. | `hole-rule-applied-to-the-hole-s-own-pad` in the failure corpus |
| 25 | The router halo (#21's sibling fix) was applied to **all four sides**, which put a USB-C receptacle's cable-insertion datum millimetres inside the outline — a socket no cable reaches. The halo's evidence is about interior blocks; there is nothing to route in front of a connector. | `place_board` regression | **`circuitlib`** — bottom-edge connectors use the dedicated `EDGE_CONNECTOR_INSET_MM` insertion allowance on the connector face while the other three sides retain the interior margin. | `EDGE_CONNECTOR_INSET_MM if block_id in EDGE_BLOCKS else margin` in `layout.py` |
| 9 | Silkscreen plotted at 0.033mm strokes; JLCPCB holds 0.15mm and thinner ink prints broken or not at all. **The real defect was worse than the symptom: all 54 silkscreen texts on the converted board are under JLCPCB's 1.0mm minimum height, 39 of them at 0.267mm, and none carries an explicit stroke — so KiCad derives one and plots it at a fifth of the floor. Every board this tool has ever made arrives with no readable reference designator.** | all three | **Pipeline (stage 3a)** — `kicad_normalize.normalize_for_fab` holds the converted board to the fab profile's silkscreen floors between the converter we do not own and the plotter. Idempotent, counted, reported. Measured after: 1864 of 1865 sub-floor strokes → 0 | `normalize_for_fab` in `circuitpy/kicad_normalize.py` |
| 16 | Raising silkscreen to the printable floor made 99 strokes cross exposed-pad mask openings. Ink there can prevent wetting; trusting a board house to silently repair the packet is not a fab-ready process. | all three | **Shipping export + independent Gerber gate** — KiCad plots with `--subtract-soldermask`, so no silk is emitted where copper is exposed, and `gerber_silk_over_pad` independently blocks any overlap that survives. This was exercised through real KiCad 10 Gerbers, not inferred from the command line. | `_kicad_gerber_export_args` in `generation.py`; `gerber_silk_over_pad` in `fab.py` |
| 26 | Mask subtraction made soldering safe but could erase part of a badly placed reference: a real KiCad 10 fixture removed 2.9661mm, or 29.9%, of `R123` while every old gate passed. The Gerber parser also discarded `%LPD/%LPC`, so it mistook the negative pad flash for positive content and could not reason about the final printed image. | all three | **Pipeline + independent Gerber gate** — after applying the 1mm/0.15mm fab floors, `normalize_for_fab` keeps a complete reference only when it is inside the outline and clear of same-face mask openings/other refs, otherwise relocates it to a deterministic nearby slot or emits blocking `silkscreen_refdes_unreadable`. The parser now retains polarity and plot order; a negative flash suppresses a finding only when its standard aperture provably erases the complete intersecting ink. Real fixture after: plain plot 18 overlaps, subtracted plot 0; relocated board plots through KiCad 10 and the second normalization is a no-op. | `_relocate_references` / `unreadable_findings` in `kicad_normalize.py`; polarity/sequence in `verifylib/gerber.py` |
| 28 | A sidecar recorded only upstream package versions even though our audited router and verifier safety fixes patch those exact pinned bundles without changing semver. Source-identical copper or an acceptance verdict from before a clearance, via, plane, differential-pair, or source-trace width fix could therefore look current; the example ratchet also trusted whichever committed sidecar and mutable block copy happened to be present. | all three | **Pipeline + evidence lock** — sidecars/export cache identity include exact checks/core/capacity/props bundle SHA-256 values; source, pipeline, toolchain, and content-hashed golden-block snapshot drift invalidate reuse. The deterministic block sync preserves all selected bytes, including third-party license/provenance, and refuses to overwrite local edits. `examples_lock.py`, CI, and `review-packet` refuse stale/missing/incomplete evidence, and `--accept` refuses both stale evidence and a real blocking/fab-readiness regression. | `checksBundleSha256` / `coreBundleSha256` in `toolchain.py`; `InvalidGoldenBlockSnapshot` in `evals/examples_lock.py`; `test_refresh_refuses_to_overwrite_a_modified_frozen_snapshot` |
| 34 | The source trace for a 0.8mm authored power trunk was connected to a 0.2mm local neck, and the width checker measured the thinnest copper anywhere on the electrical net. It falsely reported every exact 0.8mm trunk as 0.2mm, creating twelve blockers on the coherent RP2040 preflight and making mixed-width authored trees impossible to verify honestly. | golden RP2040 block | **Pinned verifier** — `@tscircuit/checks` first measures every PCB route carrying the exact source trace ID; connectivity-wide selection is retained only when no exact identity exists. Multiple routes claiming the same ID are all measured, so an accidentally duplicated 0.6mm route still fails. Sidecars now pin the checks-bundle hash as part of the same correction. | `CHECKS_SOURCE_TRACE_WIDTH_IDENTITY_PATCH`; `checks-source-trace-width-identity.test.mjs`; `checksBundleSha256` |
| 35 | Golden block APIs could change without the planner registry noticing because exact prop equality covered only four hand-picked blocks. `SwTact` already exposed the expert-requested compact `variant`, per-trace width, and layer, while the planner omitted all three and could generate only the stale surface. | Terminal review; registry audit | **Planner registry + structural eval** — every plannable block now has exact set equality between live exported TSX props and registry props. Every composition constructor must supply all required props and may supply no removed/unknown prop. `SwTact` exposes `variant`, `signalTraceWidthMm`, and `layer` through circuitlib. | `test_migrated_registry_props_match_live_golden_exports`; `test_composition_supplies_every_required_golden_prop` over `REGISTERED_BLOCKS` |
| 39 | The golden routing-contract inventory decided whether a bench routed by searching the entire source for the string `routingDisabled`. A comment, literal false, or the normal default-routed wrapper `props.routingDisabled ?? false` could therefore remove real routed copper from the authoritative/blocker set while its zero-prop export still ran the router. The gauntlet separately removed that word globally, which could mutilate a wrapper into invalid JSX. | prevention audit | **Golden acceptance gate** — one self-contained JSX helper parses the actual `<board>` opening tag. Comments and false/default-false controls remain routed; only definite true is geometry-only. The gauntlet rewrites only bare/true to false, preserves already-routed forms byte-for-byte, and rejects dynamic controls instead of guessing. | routing-enabled bench set equality; `test_routing_classification_reads_the_board_prop_not_a_substring`; gauntlet rewrite/refusal cases |
| 40 | The composition matrix labelled a build `clean` and returned success when it had zero validation errors, even if `fab.ready` was false because the manufacturing packet was incomplete or not produced by the accepted substrate. Its progress line said FAIL, but its final exit said success. The full block gauntlet made the same error-only check despite claiming fab-ready closure. | prevention audit | **Composition + golden acceptance gates** — matrix `clean` requires both zero blockers and literal `fab.ready == true`; a blocker-free but non-ready cell is `not-ready`, carries `NotFabReady`, and exits non-zero. Every block gauntlet likewise requires fab readiness and content-verifies its selected Circuit JSON against routing-attempt evidence. The one intentionally unsupported USB pair is pinned at the earlier planner-law boundary rather than falsely described as a routed matrix cell. | composition clean/not-ready unit; unsupported planner-law unit; `test_block_survives_the_board_gauntlet` |
| 41 | A fresh `npm ci` restored the pristine capacity bundle, then the guarded patch chain stopped halfway because a replacement payload had gained one extra newline while its declared successor SHA remained unchanged. The patcher correctly refused the unknown output, but the focused test only checked an already-patched install, so app restart—not review CI—found the inconsistency. | fresh `scripts/dev.sh` restart | **Patch manifest + source test** — reconstruct npm-pristine capacity bytes from any recognized pristine/intermediate/final installed endpoint, replay every exact stage into a private package, assert each successor SHA, then re-run every stage check-only for final idempotency. A byte of whitespace in a replacement payload now fails the focused source test without mutating shared `node_modules`. | `the complete capacity chain replays from pristine bytes after a restart` in `toolchain-patches.test.mjs`; exact patch-chain SHA guards |
| 43 | Plane-terminated one-port traces retained their exact source widths in SRJ (`0.20`, `0.40`, and `0.15mm`) but the fanout solver collapsed every dogbone to the phase-global `0.15mm`. The ordinary width checker also missed the one-port undercut, so parsed-clean output could contain undersized rail copper. | compact RP2040 local-rail probe | **Pinned router + output boundary** — plane buses preserve each exact connection's hard width through assignment, collision scoring, and emitted copper. An algorithm-independent core gate rejects fresh or cached output whose exact source trace is thinner than requested, and the new semantics are part of route-cache identity. Ordinary no-width GND fanout retains the board default. | `CORE_PLANE_FANOUT_CONNECTION_WIDTH_PATCH`; `plane-fanout-trace-width.test.mjs` mixed-width top/bottom, exact-DRC, tampered-output, invalid-width, and cache-key cases |
| 44 | Gerber reconciliation discarded `pcb_smtpad.is_covered_with_solder_mask`, then falsely demanded solder-mask and paste apertures for hidden one-port routing nodes. A real starter packet accumulated six blocking Gerber findings even though its intended mask-covered copper was correct. | generated protected starter | **Independent Gerber truth model** — Circuit JSON ingestion preserves only the literal `is_covered_with_solder_mask === true` flag. Such a pad still must exist in copper, but it is not a solderable assembly landing and therefore needs neither a mask opening nor paste. Unflagged pads—including arbitrary DNP pads—retain every mask/paste requirement. | commit `80d0cd0`; `test_an_explicit_mask_covered_routing_node_needs_copper_but_not_mask_or_paste`; full verify suite 187 passed and starter Gerber errors 6→0 |
| 45 | `parts.json` could name a supplier part no populated compiled BOM row consumed, yet assembly still looked orderable. Worse, the three legacy examples used `{version, summary, parts:[...]}` while the loader silently discarded the list and retained `summary` as a pseudo-part, so a later stale-lock check could validate neither the intended refs nor the intended schema. | generated starter and three legacy examples | **Spec boundary + BOM gate** — a present lock must be one exact uppercase component-ref → object map; legacy wrappers, lists, lowercase/group/range/metadata/empty keys, and non-object entries fail before tooling. After compiled DNP filtering, every lock ref must match a populated BOM row: stale/drift/ambiguous identities block assembly and advise bare PCB. | `test_legacy_parts_book_wrapper_is_rejected`; `test_lock_keys_are_single_exact_uppercase_component_refs`; `test_stale_parts_lock_entry_blocks_assembly`; `test_filtered_dnp_lock_entry_is_stale`; unconditional `parts_lock=parts` BOM gate call |
| 38 | Routing retries recorded hashes and blocker counts, but deleted the non-selected completed candidate and never independently reproduced either candidate's scan. A forged Circuit JSON plus a forged, schema-shaped scan and matching hashes could therefore pass structural publication checks. Raw verifier order and duplicates could also bias which attempt looked better. | routing-evidence adversarial audit | **Pipeline + publication evidence** — the canonical, exact-deduped and sorted pre-export scan now drives attempt selection. Every completed candidate retains content-addressed Circuit JSON and canonical scan bytes; reuse, the example lock, and review publication hash and parse every retained record and independently rerun harvest, pinned checks, IoU, and DFM under the current product/profile. Attempts, sidecar, and selected IR publish as one rollback-safe transaction that prunes stale files only after success; failed attempts remain minimal, and selected retained bytes equal `main.circuit.json`. | `RoutingEscalation` missing/mutation/forged-scan/noncanonical/selected-drift cases; attempt-set and late sidecar/final-IR rollback; example-lock retained mutation; review-packet self-consistent forgery negative |
| 21 | A blanket policy made every board start at `5x`, based on the same cache-contaminated evidence as #4. Effort is not a fab-readiness guarantee: a harder route can be much slower and still preserve an impossible placement or topology. | rp2040-core bench; all three boards | **Pipeline + skeleton** — generated boards start at the router default. Only a parsed routing-class blocker permits one bounded, cold, configuration-keyed 5x alternate candidate; authored effort and routing-disabled boards are never overwritten, and the retry is retained only when its independently scanned artifact has fewer blocking findings. The project skeleton contains no universal effort prop. | `RoutingEscalation` tests; `test_effort_change_is_a_cold_configuration_keyed_comparison`; absence of `autorouterEffortLevel` from the skeleton |
| 30 | The product declared a 400.6–500mA USB current-limit range and the artifact gate proved the TPS2553 identity and IN/OUT nets, but never inspected the 59kΩ ILIM resistor that actually programs that range. A missing, wrong-value, or wrong-net R31 could pass while making the declared limit fictitious. | harness-puck | **Block + planner + independent artifact gate** — `UsbSourceContract` owns the setting pin and exact R31 identity/value/return topology, `usb_power_budget_for_plan()` emits them, and `power_intent` blocks a missing/DNP/wrong-part/wrong-value/wrong-topology setting network. | `settingResistor` in `circuitlib/helpers.py`; `power_intent_usb_limiter_setting_topology` in `verifylib/power_intent.py` |
| 31 | `usb-power-entry` passed its isolated route bench but failed immediately in the composition matrix at the product's 0.15mm trace-to-pad floor: its 0.20mm EN branch had only 0.2475597mm centreline distance from U7.GND where 0.2500000mm was required. The isolated bench had silently used the looser runtime default, so it proved a different board contract. | protected USB composition | **Block + golden gauntlet** — the block owns a transform-safe, via-free EN dogleg; both face benches explicitly carry the product's 0.15mm trace-to-pad and via-to-pad floors. Every routing-enabled golden bench is now classified: an authoritative bench must state both real floors and compile clean, while an unresolved route remains a named blocker instead of quietly reverting to a weaker board. The emitted EN paths are exact mirrors, 0.20mm wide, and 2.182621mm against the 3mm limit. | `TR_U7_en` in `usb-power-entry.tsx`; `test_usb_power_entry_bottom_preserves_power_fault_contract`; `test_routing_board_contracts.py` |
| 32 | A generic 2.0mm decoupling preference was enforced as though it were an RP2040 manufacturer requirement. It made the north-side bypass/QSPI geometry mathematically impossible and sent repeated searches after a nonexistent placement. Raspberry Pi's routed Minimal R3-S1 reference measures 2.618–4.630mm from several RP2040 supply-pad centres to their nearest bypass-pad centres in order to preserve the direct flash corridor. | golden RP2040 block | **Product contract + reusable block** — products retain the strict default but can declare a populated-ref-scoped, manufacturer-cited `decoupling.overrides` distance. Schema and independent verification reject malformed, unmatched, exclusion-conflicting, and uncited overrides; topology, measurable pads, explicit trace bounds, routed copper, and DRC remain mandatory. The RP block must use the cited 5mm envelope and complete reference-cluster topology rather than exclude the MCU or weaken unrelated parts. | `decoupling.overrides` in `layout_intent.py` and `verifylib.intent`; Raspberry Pi `Minimal-KiCAD.zip` measurement regression |
| 33 | CI described KiCad as the substrate that makes a packet shippable, then installed it on a best-effort basis and treated absence as a passing degraded-path exercise. The main circuitpy and golden jobs did not install it at all, so conversion, independent KiCad DRC, mask-subtracted Gerbers, and hidden-node KiCad boundaries could silently skip while the pipeline stayed green. | pipeline CI | **Required second substrate** — pipeline, golden-block, and structural-eval jobs install the supported KiCad 10 release from KiCad's official Ubuntu PPA and prove `kicad-cli version` before running tests. Developer machines may still skip unavailable integration tests, but authoritative CI cannot. | the three fail-closed `Install KiCad CLI` steps in `.github/workflows/ci.yml`; real `test_kicad.py` and hidden-node Gerber integration |
| 8 | **The debug interface (SWCLK, SWD) reached no connector and no test point — the assembled board could not be halted, single-stepped or recovered from a bad image.** | all three | **Planner + reusable block contract.** `rp2040-core` declares `exposes=("SWCLK","SWD")`; `unexposed_nets()` reports what nothing brings out; `validate_board_law` makes it an **error**; and `BoardPlan.buildable` is false until the consumer passes the nets its real connector/probe physically exposes. `DebugPort` supplies three 2.54mm DNP pads for SWCLK/SWD/GND. `Rp2040Core` owns that electrical furniture structurally but requires the consuming board to provide `debugPortPcbX` / `debugPortPcbY`; a universal core-local position was proven unsafe when Hydrate's 180-degree parent transform put the pads inside its USB connector. The board therefore owns collision-free physical placement and cannot silently omit the port. | `BoardPlan.buildable` / `board_plan(exposed_nets=...)` in `circuitlib/helpers.py`; `test_the_plan_says_what_must_come_out` |
| 13 | Copper sized for 604mA carrying 649mA, every segment one width. | harness-puck | **`circuitlib`** — `Block.peak_draw_ma` / `peak_per_unit_ma` give the planner a worst case (a WS2812 idles at 4mA and pulls 60mA), `board_plan` turns it into `power_trace_width_mm` by IPC-2221, and declaring less is an error | `power_trace_width` in `circuitlib/helpers.py` |
| 14 | An LDO reaching ~96 degC junction at the downstream chain's datasheet peak. | harness-puck | **Planner** — the same peak drives `regulator_thermal` at a 45 degC ambient, and a plan that would cook the part has `buildable = False`. Twenty WS2812s behind an AMS1117 is 1.3A and 183 degC: that board can no longer be planned | `overheats` in `circuitlib/helpers.py` |
| 15 | The pour-margin fix from #2 was structural but the number was still written down in four places: `hydrate-coaster` hardcoded `cutoutMargin="0.25mm"` on two pours, while the USB-C block guide and review repeated the literal as advice. A copied manufacturing number will drift. | hydrate-coaster | **Delete the copies** — the pours consume `POUR_CUTOUT_MARGIN_MM`, and the guide/review point at `GndPlanes` or the constant rather than quoting its value. `scripts/shift-left-check` contains positive-use and forbidden-literal ratchets. | no hardcoded cutout literal in the product source or reusable guidance |
| 10 | Mask openings separated by less than the 0.2mm sliver minimum; narrowest 0.114mm. | all three | **Not a defect — the check was wrong.** All ten sub-0.2mm webs sit inside *one part's own land pattern*: 0.114 and 0.157mm inside the USB-C receptacle's footprint, 0.1985mm inside each of eight 0402s, which is simply what a 0402 land pattern is. Those dams are specified by the package and JLCPCB builds them daily. The check is now scoped to webs between *different* parts — where nobody qualified the geometry — and that version fires on none of the three boards. Escalating the original would have made every board permanently un-orderable over a standard passive | `_mask_slivers` scoping in `verifylib/gerber_truth.py` |

---

## Open — detected, but a user can still hit them

These are the ones that matter now. Each is currently check-only.

| # | Defect | Found on | Where the fix belongs |
|---|---|---|---|
| 4 | The claimed `autorouterEffortLevel="5x"` improvement was not a controlled comparison. Core's route-cache identity omitted effort and pipeline, and stage 0b did not clear its private cache, so terminal-keyboard 46 → 18 and harness-puck 5 → 1 may compare stale/reused copper rather than default against `5x`. Cache isolation is now fixed, but those measurements cannot prove that `5x` improves a future dense board. | all three | **Pipeline + gauntlet.** Re-measure cold, configuration-isolated attempts on representative dense boards and select only from parsed final artifacts; keep the retry bounded. |
| 12 | USB pair skew/uncoupled copper exceeded its physical budget. Native pair rules now survive exact trace selection and a separately phased adjacent orientation edge, the numerical zero-edge crash is fixed, and the rebuilt reusable USB block routes both faces within its 3mm uncoupled and 3.8mm skew limits. The products still vendor the older aggregate/named-net composition, however, so no dense consumer artifact yet proves that corrected physical contract. A clean isolated block is not a routed product. | terminal-keyboard; golden USB composition | **Planner + block + router gauntlet** — sync the direct flow-through block into every consumer and require the native solver to satisfy skew/uncoupled/DRC on each dense-board fixture; never raise or omit the declared limits. |
| 27 | A capacitor existed on the right rail, but both the IC pin and capacitor were independent `port -> net` leaves, so the MST—not the block—designed the bypass loop. The published artifacts contain 13 such topology failures on Terminal, 13 on Hydrate and 22 on Harness under the new 2mm contract. Golden sensor, LED, USB-entry, LDO, and pixel blocks now own bounded local trees, but the products still vendor older copies and generic chip composition can still emit the bad topology. | all three | **Block / planner.** Sync the frozen bounded pin-to-cap branches and sole authored rail boundaries into every consumer, then make generic powered-chip composition emit the same topology before routing. The product gate already blocks missing/distant/MST-only loops, but detection alone still costs a failed build. |
| 36 | The public project skeleton says `product.json` is the physical and power contract, but the file contains only name/power/envelope/layers/fab/assembly. Its board directly composes an unprotected USB source to an LDO and declares neither generated `layout`/`powerBudget`, authored board power trees, nor ground planes. Circuitlib helper APIs and downstream checks exist, but prose asking an agent to replace the starter is not automatic prevention. | generated starter audit | **Planner / project generator / skeleton.** Generate the starter from a buildable `board_plan`, `product_layout`, and `usb_power_budget_for_plan`; content-lock the selected blocks and emit protected USB, authored local/tree power topology, ground planes, and real board floors. A cold routing-enabled generated project must pass parsed intent, KiCad/fab, selected-attempt, and review-publication gates without hand repair. |
| 37 | A faithful dense RP power graph can remain in the schematic `TraceOverlapShiftSolver`; `schematicDisabled` makes the timeout disappear but also removes the deliverable. A fixed-physical fixture proves `schAutoLayoutEnabled` completes with real schematic components/traces, yet no skeleton, planner, or canonical RP consumer mechanically selects that supported mode. | RP full power-tree probe | **Planner / generator, or bounded upstream schematic solver.** Select and test the supported schematic mode for dense authored rail graphs (or make the solver bounded), then require the coherent routing-enabled RP/generated consumer to retain real schematic output. The current routing-disabled positive is mitigation evidence, not product closure. |
| 42 | A mask-covered one-port plated hole can provide useful source-connected top/bottom copper, but installed `@tscircuit/checks` excludes `pcb_plated_hole` from different-net via spacing and measures drill edges rather than both outer annuli. The exact RP candidate is already invalid under circuitpy's independent PTH-to-copper and annular-ring floors, so this omission does not make that topology green. The unclosed band is a geometrically legal drill clearance whose via/PTH outer-copper gap still undercuts the board's declared 0.15mm. | RP plated-node probe | **Independent checker / minimized corpus.** Add different-net via↔PTH and PTH↔PTH outer-copper negatives plus same-net and exact-legal twins; then enforce the board-declared copper-edge clearance without replacing the distinct drill/annular manufacturing rules or double-counting one pair. Keep full circuitpy/KiCad evidence mandatory; never infer physical0 from installed-checks0 alone. |

---

## What these three boards taught about checks themselves

Not defects in a board — defects in the *way we look at boards*. Both were
caught before shipping, and both would have been invisible afterwards.

| # | Lesson | How it surfaced | What changed |
|---|---|---|---|
| A | **A check that cannot parse a shape silently passes it.** Our gerber reader could not evaluate parameterised aperture macros (`RotRect`, `RoundRect` — defined once, given their real numbers at each `%ADD%`), so it assigned them zero size and dropped them. **56 of 230 mask openings on harness-puck — a quarter of the layer, and every fine-pitch QFN pad — were never examined.** The sliver count went from "2" to the true 10 once they were resolved. | measuring a fix's before/after and finding the two numbers incomparable | macro parameters are substituted and rotation applied; nothing is dropped for being unparseable |
| B | **A fix can blind its own smoke alarm.** Setting `solder_mask_min_width` made KiCad plot the mask as 223 filled regions instead of 174 flashed apertures. The check only understood flashes, so it would have reported a clean mask on every board where the fix had been applied — quiet, not clean. | re-running the check on the fixed packet before believing the fix | the reader handles both representations, and the setting itself was **retracted**: measured against the same yardstick it removes no web at all, and costs an 8x slowdown. A change that alters the representation and not the board is a placebo |
| C | **Two serialization paths for the same copper must join to the same net.** Autorouted traces carry `connection_name`; fixed `pcbPath` traces carry `source_trace_id`. Reading only the first made Hydrate's VBUS crossover, power trunks and debug escapes disappear from every independent net-class/ground-length measurement. | the explicit Hydrate artifact test saw a long narrow V5 path while product intent reported no V5 trace at all | `verifylib.model.Board` resolves `connection_name or source_trace_id`; regressions prove both the model join and a fixed narrow power-trunk rejection |

## The rule

When a board fails and an agent fixes it, the question is never "is the board
fixed". It is **"why did the first composition get this wrong, and what stops
the next one"**. The fix belongs in the planner, the block, the skeleton or
`circuitlib` — never in advice a user has to remember, and never only in the
example that happened to catch it.

A block that needs correct handling to be safe is a block that will be handled
wrong.
