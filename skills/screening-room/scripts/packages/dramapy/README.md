# Vendored dramapy (generated — do not hand-edit)

This directory is the screening-room skill's self-contained copy of
`packages/dramapy/src/dramapy/`, populated at build time by
`scripts/build/build-skill-runtimes.sh` (rsync, `__pycache__` excluded).

The skill imports `dramapy.review_bundle` from here at runtime so it never
reaches outside its own directory. **Only this README.md and `.gitignore` are
tracked; the rest of the tree is gitignored and regenerated on every build.**

To refresh it locally: `scripts/build/build-skill-runtimes.sh`.
